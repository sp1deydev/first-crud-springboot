server:
  port: 8080
  servlet:
    context-path: /demo


spring:
    datasource:
      url: "jdbc:mysql://localhost:3306/demo"
      username: root
      password: root
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

jwt:
  secret-key: "test_spring_boot_test_secret_key"
  access-token-exp: 20 #seconds
  refresh-token-exp: 120 #seconds